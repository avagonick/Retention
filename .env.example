# Copy this file to .env and fill in your values. .env is gitignored.

# Lightning AI TRIBE v2 deployment URL (rotates per cloudspaces session).
TRIBE_BASE_URL=https://your-deployment.cloudspaces.litng.ai

# Required: API token sent as the X-API-Token header. Keep this secret.
TRIBE_API_TOKEN=your-api-token-here
